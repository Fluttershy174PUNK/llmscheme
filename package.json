{
	"name": "llmscheme",
	"version": "2.0.0",
	"private": true,
	"type": "module",
	"description": "Living logic schemes for humans and LLM — one format, three consumers (skill / service / editor) over one core.",
	"license": "MIT",
	"engines": {
		"node": ">=22.18"
	},
	"scripts": {
		"build": "node src/build/build.ts",
		"sync-skill": "node src/build/sync-skill.ts",
		"check": "node src/build/check.ts",
		"typecheck": "tsc --noEmit",
		"test": "node --test \"src/test/**/*.test.ts\"",
		"verify": "npm run typecheck && npm test && npm run build && npm run check"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"typescript": "5.9.3"
	}
}
